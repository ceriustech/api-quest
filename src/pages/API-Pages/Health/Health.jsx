import React from "react";

const Health = (props) => {
  console.log(props);
  return (
    <div className="main-content_wrapper health">
      <h1>Health Page</h1>
    </div>
  );
};

export default Health;
